id: readme-demo
title: README evidence-workflow demo GIF
owner: "@samotest"
tags: ["docs", "demo", "gif", "evidence-workflow"]
priority: required
target:
  url: "http://127.0.0.1:43733/docs/demo.html"
recording:
  mode: browser
  duration_ms: 4500
  output_path: docs/demo.gif
  preferred_formats: ["gif"]
steps:
  - id: capture-evidence-workflow-demo
    instruction: "Record the README demo page showing the evidence workflow."
    expected: "The GIF shows record, inspect, and gate handoff concepts."
    evidence:
      - type: gif
        name: readme-demo
result:
  required_observations:
    - "The demo shows the evidence workflow, not just scrolling help text."
